<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group
        android:id="@+id/fragment_selector_group"
        android:checkableBehavior="single">
        <item
            android:id="@+id/players"
            android:icon="@drawable/ic_players_icon"
            android:title="@string/players" />
        <item
            android:id="@+id/map"
            android:icon="@drawable/ic_map_black_24dp"
            android:title="@string/map" />
        <item
            android:id="@+id/cards"
            android:icon="@drawable/ic_tickets"
            android:title="@string/tickets" />
        <item
            android:id="@+id/scoreboard"
            android:icon="@drawable/ic_view_list_black_24dp"
            android:title="@string/scoreboard" />
    </group>

    <!--
        <group
            android:id="@+id/extras_group"
            android:checkableBehavior="single">
            <item
                android:id="@+id/settings"
                android:icon="@drawable/ic_menu_manage"
                android:title="@string/settings" />
            <item
                android:id="@+id/about"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/about" />
        </group>
    -->


</menu>
